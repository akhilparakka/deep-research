[project]
name = "deep-research"
version = "0.1.0"
description = "Deep research using LLM"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain-openai",
    "langgraph",
    "python-dotenv"
]

[tool.setuptools]
packages = ["src"]
